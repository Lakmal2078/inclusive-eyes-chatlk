const state={token:localStorage.getItem('chatlk_token'),user:JSON.parse(localStorage.getItem('chatlk_user')||'null'),chats:[],contacts:[],active:null,messages:[],socket:null,groupMembers:[]};
const app=document.querySelector('#app');
const api=async(path,options={})=>{const response=await fetch(`/api${path}`,{...options,headers:{'Content-Type':'application/json',Authorization:state.token?`Bearer ${state.token}`:'',...(options.headers||{})}});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Request failed');return data};
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function auth(){app.innerHTML=`<section class="auth-screen"><div class="card"><div class="brand">✦ ChatLK</div><p>Private, real-time conversations.</p><form id="login"><div class="field"><label>Username or phone</label><input name="identifier" required></div><div class="field"><label>Password</label><input name="password" type="password" required></div><button class="primary">Sign in</button></form><p>New here? <button class="link" id="show-register">Create an account</button></p><p class="error" id="error"></p></div></section>`;document.querySelector('#login').onsubmit=login;document.querySelector('#show-register').onclick=register}
function register(){app.innerHTML=`<section class="auth-screen"><div class="card"><div class="brand">✦ ChatLK</div><form id="register"><div class="field"><label>Username</label><input name="username" pattern="[A-Za-z0-9_]{3,20}" required></div><div class="field"><label>Phone (E.164)</label><input name="phone" placeholder="+94771234567" required></div><div class="field"><label>Display name</label><input name="display_name" required></div><div class="field"><label>Password</label><input name="password" type="password" minlength="8" required></div><button class="primary">Create account</button></form><p>Already registered? <button class="link" onclick="location.reload()">Sign in</button></p><p class="error" id="error"></p></div></section>`;document.querySelector('#register').onsubmit=async e=>{e.preventDefault();try{const data=await api('/auth/register',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});saveSession(data);render()}catch(error){document.querySelector('#error').textContent=error.message}}}
async function login(e){e.preventDefault();try{const data=await api('/auth/login',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});saveSession(data);render()}catch(error){document.querySelector('#error').textContent=error.message}}
function saveSession(data){state.token=data.token;state.user=data.user;localStorage.setItem('chatlk_token',state.token);localStorage.setItem('chatlk_user',JSON.stringify(state.user))}
async function render(){if(!state.token)return auth();try{const [chatData,contactData]=await Promise.all([api('/chats'),api('/contacts')]);state.chats=chatData.chats||[];state.contacts=contactData.contacts||[]}catch{state.token=null;return auth()}app.innerHTML=`<div class="shell"><aside class="sidebar"><header><strong>ChatLK</strong><div><button class="new-chat" id="new-chat">＋ New</button><button class="link" id="logout">Log out</button></div></header><div class="sidebar-actions"><button class="outline-btn" id="contacts-btn">Contacts</button><button class="outline-btn" id="group-btn">＋ Group</button></div><input class="search" id="search" placeholder="Search chats"><div id="chat-list"></div></aside><section class="main"><header class="topbar"><div class="avatar">${esc((state.user?.display_name||state.user?.displayName||'U')[0])}</div><div><strong id="chat-title">Select a chat</strong><div id="presence">Ready</div></div></header><div class="messages" id="messages"><div class="empty">Choose a conversation to begin.</div></div><form class="composer" id="composer"><input name="text" placeholder="Write a message..." autocomplete="off"><button class="send">Send</button></form></section></div>`;document.querySelector('#logout').onclick=()=>{localStorage.clear();location.reload()};document.querySelector('#new-chat').onclick=showNewChat;document.querySelector('#contacts-btn').onclick=showContacts;document.querySelector('#group-btn').onclick=showGroup;document.querySelector('#composer').onsubmit=send;drawChats()}
function drawChats(){const list=document.querySelector('#chat-list');if(!state.chats.length){list.innerHTML='<p class="empty">No conversations yet.<br>Tap <b>＋ New</b> to begin.</p>';return}list.replaceChildren();for(const chat of state.chats){const item=document.createElement('button');item.className='chat-item';item.innerHTML=`<span class="avatar">${esc((chat.name||'C')[0])}</span><span class="chat-meta"><strong>${esc(chat.name||'Direct chat')}</strong><span>${esc(chat.last_message||'No messages yet')}</span></span>`;item.onclick=()=>openChat(chat);list.append(item)}}
function modal(title,body){const modal=document.createElement('div');modal.className='modal-backdrop';modal.innerHTML=`<section class="modal card"><h2>${title}</h2>${body}<button class="link" id="close-modal">Cancel</button></section>`;document.body.append(modal);modal.querySelector('#close-modal').onclick=()=>modal.remove();return modal}
function showNewChat(){const modal=modalFactory('Start a new chat',`<p>Search by username, phone, or display name.</p><input class="search" id="user-query" placeholder="e.g. user_name or +9477..." autofocus><div id="user-results"><p class="empty">Type at least 2 characters.</p></div>`);const query=modal.querySelector('#user-query');query.oninput=searchUsers}
function modalFactory(title,body){return modal(title,body)}
let searchTimer;function searchUsers(event){clearTimeout(searchTimer);const value=event.target.value.trim();const results=event.target.closest('.modal').querySelector('#user-results');if(value.length<2){results.innerHTML='<p class="empty">Type at least 2 characters.</p>';return}searchTimer=setTimeout(async()=>{try{const data=await api(`/users/search?q=${encodeURIComponent(value)}`);results.replaceChildren();if(!data.users?.length){results.innerHTML='<p class="empty">No users found.</p>';return}for(const user of data.users){const button=document.createElement('button');button.className='chat-item';button.innerHTML=`<span class="avatar">${esc((user.display_name||user.username)[0])}</span><span class="chat-meta"><strong>${esc(user.display_name||user.username)}</strong><span>@${esc(user.username)}</span></span>`;button.onclick=()=>createDirectChat(user);results.append(button)}}catch(error){results.innerHTML=`<p class="error">${esc(error.message)}</p>`}},250)}
async function createDirectChat(user){try{const data=await api('/chats',{method:'POST',body:JSON.stringify({type:'direct',participantIds:[user.id]})});document.querySelector('.modal-backdrop')?.remove();await refreshChats();const chat=state.chats.find(item=>item.id===data.id);if(chat)openChat(chat)}catch(error){const results=document.querySelector('.modal-backdrop #user-results');if(results)results.innerHTML=`<p class="error">${esc(error.message)}</p>`}}
async function refreshChats(){const data=await api('/chats');state.chats=data.chats||[];drawChats()}
async function refreshContacts(){const data=await api('/contacts');state.contacts=data.contacts||[]}
function showContacts(){const body=`<p>Add people by username or phone. You can block or remove saved contacts.</p><form id="add-contact" class="inline-form"><input name="identifier" placeholder="username or +9477..." required><input name="nickname" placeholder="Nickname (optional)"><button class="primary">Add contact</button></form><p class="error" id="contact-error"></p><div id="contact-list"></div>`;const m=modal('Contacts',body);m.querySelector('#add-contact').onsubmit=addContact;drawContacts(m)}
function drawContacts(modal){const list=modal.querySelector('#contact-list');if(!state.contacts.length){list.innerHTML='<p class="empty">No contacts saved yet.</p>';return}list.replaceChildren();for(const contact of state.contacts){const item=document.createElement('div');item.className='contact-row';item.innerHTML=`<span class="avatar">${esc((contact.display_name||contact.username)[0])}</span><span class="chat-meta"><strong>${esc(contact.nickname||contact.display_name)}</strong><span>@${esc(contact.username)} · ${contact.is_blocked?'Blocked':'Active'}</span></span><button class="outline-btn contact-chat">Chat</button><button class="danger-btn contact-block">${contact.is_blocked?'Unblock':'Block'}</button><button class="danger-btn contact-remove">Remove</button>`;item.querySelector('.contact-chat').onclick=()=>createDirectChat(contact);item.querySelector('.contact-block').onclick=()=>updateContact(contact.id,!contact.is_blocked,modal);item.querySelector('.contact-remove').onclick=()=>removeContact(contact.id,modal);list.append(item)}}
async function addContact(event){event.preventDefault();const modal=event.target.closest('.modal');try{await api('/contacts',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(event.target)))});await refreshContacts();event.target.reset();drawContacts(modal)}catch(error){modal.querySelector('#contact-error').textContent=error.message}}
async function updateContact(id,isBlocked,modal){try{await api(`/contacts/${encodeURIComponent(id)}`,{method:'PATCH',body:JSON.stringify({is_blocked:isBlocked})});await refreshContacts();drawContacts(modal)}catch(error){modal.querySelector('#contact-error').textContent=error.message}}
async function removeContact(id,modal){try{await api(`/contacts/${encodeURIComponent(id)}`,{method:'DELETE'});await refreshContacts();drawContacts(modal)}catch(error){modal.querySelector('#contact-error').textContent=error.message}}
function showGroup(){state.groupMembers=[];const body=`<p>Select contacts or search users to add members.</p><div class="field"><label>Group name</label><input id="group-name" maxlength="120" placeholder="e.g. Family"></div><input class="search" id="group-query" placeholder="Search users"><div id="group-selected" class="selected-users"></div><div id="group-results"><p class="empty">Select at least one other user.</p></div><p class="error" id="group-error"></p><button class="primary" id="create-group">Create group</button>`;const m=modal('Create a group',body);m.querySelector('#group-query').oninput=e=>searchGroupUsers(e,m);m.querySelector('#create-group').onclick=()=>createGroup(m);drawSelected(m)}
function drawSelected(modal){const box=modal.querySelector('#group-selected');box.replaceChildren();for(const user of state.groupMembers){const chip=document.createElement('span');chip.className='user-chip';chip.textContent=user.display_name||user.username;const remove=document.createElement('button');remove.textContent='×';remove.onclick=()=>{state.groupMembers=state.groupMembers.filter(item=>item.id!==user.id);drawSelected(modal)};chip.append(remove);box.append(chip)}}
let groupTimer;function searchGroupUsers(event,modal){clearTimeout(groupTimer);const value=event.target.value.trim();const results=modal.querySelector('#group-results');if(value.length<2){results.innerHTML='<p class="empty">Type at least 2 characters.</p>';return}groupTimer=setTimeout(async()=>{try{const data=await api(`/users/search?q=${encodeURIComponent(value)}`);results.replaceChildren();for(const user of data.users||[]){if(state.groupMembers.some(item=>item.id===user.id))continue;const button=document.createElement('button');button.className='chat-item';button.innerHTML=`<span class="avatar">${esc((user.display_name||user.username)[0])}</span><span class="chat-meta"><strong>${esc(user.display_name||user.username)}</strong><span>@${esc(user.username)}</span></span>`;button.onclick=()=>{state.groupMembers.push(user);drawSelected(modal);button.remove()};results.append(button)}if(!results.children.length)results.innerHTML='<p class="empty">No users found.</p>'}catch(error){results.innerHTML=`<p class="error">${esc(error.message)}</p>`}},250)}
async function createGroup(modal){const name=modal.querySelector('#group-name').value.trim();if(!name||!state.groupMembers.length){modal.querySelector('#group-error').textContent='Enter a group name and select at least one member.';return}try{const data=await api('/chats',{method:'POST',body:JSON.stringify({type:'group',name,participantIds:state.groupMembers.map(user=>user.id)})});modal.remove();await refreshChats();const chat=state.chats.find(item=>item.id===data.id);if(chat)openChat(chat)}catch(error){modal.querySelector('#group-error').textContent=error.message}}
async function openChat(chat){state.active=chat;document.querySelector('.shell').classList.add('chat-open');document.querySelector('#chat-title').textContent=chat.name||'Direct chat';const data=await api(`/chats/${chat.id}/messages`);state.messages=(data.messages||[]).reverse();drawMessages();if(state.socket)state.socket.close();const protocol=location.protocol==='https:'?'wss:':'ws:';state.socket=new WebSocket(`${protocol}//${location.host}/ws?userId=${encodeURIComponent(state.user.id)}&chatId=${encodeURIComponent(chat.id)}&token=${encodeURIComponent(state.token)}`);state.socket.onmessage=event=>{const data=JSON.parse(event.data);if(data.type==='message'){state.messages.push(data.message);drawMessages()}if(data.type==='presence')document.querySelector('#presence').textContent=data.status}};
function drawMessages(){const container=document.querySelector('#messages');container.replaceChildren();for(const message of state.messages){const bubble=document.createElement('article');bubble.className=`bubble ${message.sender_id===state.user?.id||message.senderId===state.user?.id?'sent':''}`;const text=document.createElement('p');text.textContent=message.text||'[media]';const time=document.createElement('small');time.textContent=new Date(message.created_at||message.createdAt).toLocaleTimeString();bubble.append(text,time);container.append(bubble)}container.scrollTop=container.scrollHeight}
async function send(event){event.preventDefault();const input=event.target.elements.text;if(!state.active||!input.value.trim())return;const text=input.value.trim();if(state.socket?.readyState===WebSocket.OPEN)state.socket.send(JSON.stringify({type:'message',text}));else{const data=await api(`/chats/${state.active.id}/messages`,{method:'POST',body:JSON.stringify({text})});state.messages.push(data.message);drawMessages()}input.value=''}
render();
